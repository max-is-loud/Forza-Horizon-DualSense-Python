[project]
name = "src"
version = "1.1.6"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hidapi>=0.15.0",
    "psutil>=7.2.2",
    "textual>=8.2.5",
]

[tool.ruff]
line-length = 120

[tool.uv.sources]
